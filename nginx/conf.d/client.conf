server {
    listen *:52537;
    server_name localhost;

    # 前端位置配置
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ ./index.html;
    }
}
